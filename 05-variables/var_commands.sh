tf init

tf plan

tf plan -var=environment=dev
# taking higher precedence over the default value in main.tf
# takes higher over the value in terraform.tfvars


tf refresh 
# Refresh the state file with the current state of the resources in Azure
